0    logged_in				1 if successfully logged in; 0 otherwise

1    count				number of connections to the same host as the current connection in the past two seconds
9    dst_host_count			destination host's *


2    serror_rate			% of connections that have "SYN" errors		|
13   dst_host_serror_rate		destination host's *					|
												| SYN errors
3    srv_serror_rate			% of connections that have "SYN" errors (same service)|
14   dst_host_srv_serror_rate		destination host's *					|


4    rerror_rate			% of connections that have "REJ" errors		|
15   dst_host_rerror_rate		destination host's *					|
												| REJ errors
5    srv_rerror_rate			% of connections that have "REJ" errors (same service)|
16   dst_host_srv_rerror_rate		destination host's *					|


6    same_srv_rate			% of connections to the same service
11   dst_host_same_srv_rate		destination host's *

7    diff_srv_rate			% of connections to different services
12   dst_host_diff_srv_rate		destination host's *

8    srv_diff_host_rate		% of connections to different hosts (same service)

10   dst_host_srv_count		destination host's number of connections to the same service as the current connection in the past two seconds

17   protocol_type			tcp, udp, etc.
19   service				http, telnet, etc. 
25   flag				error flags, etc.
